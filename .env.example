# Copy to .env for local dev. Production values → Vercel Environment Variables.

NEXT_PUBLIC_APP_HOST=portfolio.muscadine.io

# Server-only — Vercel proxies /api/* to home API when set
API_URL=https://api.portfolio.muscadine.io
API_SECRET=change-me-session-signing-secret
